# Perl build artifacts (generated by perl Makefile.PL && make)
Makefile
# MakeMaker renames any existing Makefile aside when Makefile.PL is re-run
Makefile.old
MYMETA.json
MYMETA.yml
blib/
pm_to_blib
# left behind by `make manifest`
MANIFEST.bak
